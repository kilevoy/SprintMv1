# Core 1 v1 — контракт климатических данных

## Разделение наборов данных

### PROVEN_LEGACY_DATA

| Dataset | Доказанный источник | Что доказано | Применение v1 |
|---|---|---|---|
| `main_frame_climate_lookup` | локальные листы основной книги `снегветер` и `Города п.К`; активная цепочка `подбор`, включая lookup `снегветер!P11:X11` | данные находятся внутри основной книги и используются каркасом | экспортировать из основной книги как versioned static data; поддерживать только exact city keys, проходящие все обязательные lookup |
| `roof_load_mapping` | `подбор!V5 → W5`, lookup по `снегветер!AM:AN` | покрытие влияет на нагрузку каркаса | экспортировать вместе с climate/load data; exact key |
| `window_city_current_fixture` | `Лист1!J18=#N/A`, `K18="нет"`, `J19="нет"` для сохранённого сценария `Роза` | известен только фактический результат этого fixture | использовать в golden test, не превращать в общую таблицу городов |
| `window_wind_current_cache` | ID 4: `Ветер СП!G25=0,43969114285714284`, `G26=0,3517529142857142`, EN `D28:D29=#N/A` | четыре cached anchors старого источника | только golden fixture и диагностика; не интерполировать |

### AVAILABLE_BUT_VERSION_CONFLICT

| Dataset | Доступный кандидат | Конфликт | Правило v1 |
|---|---|---|---|
| `window_city_membership_candidate` | локальная `Таблица нагрузок по городам.xlsx`, лист `СП РК EN` | 1 142 совпадения и 244 расхождения из 1 386 проверенных ячеек; около 82,4% | не использовать как точную замену ID 3; допускается только отдельный non-parity исследовательский dataset вне Core 1 v1 |
| `window_wind_v2_candidate` | `Подбор оконых ригелей v2.0.xlsx` / соответствующие локальные листы | изменены `Расчет!D9/F9`; значения G25/G26 отличаются; добавлены компоненты нагрузки | не использовать вместо ID 4 и не смешивать с legacy fixtures |

### UNKNOWN

| Dataset / field | Почему неизвестно | Требуемая реакция |
|---|---|---|
| точная `[3]СП РК EN!B3:C1000` | отсутствует подтверждённая версия | блокировать произвольный window-city lookup |
| точная старая `[4]Ветер СП` / `Ветер по СП РК EN` | нет исходной версии и входов | блокировать пересчёт оконного ветра |
| смысл `[5]Подбор!B8=6`, `B18=66` | известны cache values, не доказана семантика/вариативность | не применять вне сохранённого cache |
| полный доказанный min/max городского списка каркаса | source docs не фиксируют полный перечень ключей | supported set формировать только механическим экспортом exact keys; неизвестный ключ отклонять |

## Схема автономного climate dataset

Каждая экспортированная запись обязана хранить:

- `dataset_id` и `dataset_version`;
- исходные `workbook`, `sheet`, `cell_or_row`;
- `city_key_raw` без нормализации орфографии;
- климатические коды и числовые нагрузки как raw typed values;
- единицы каждого числового поля;
- порядок строки для first-match;
- Excel error, если он находился в исходной ячейке;
- checksum исходной книги и checksum экспортированного набора.

Lookup выполняется exact-match, без fuzzy search, исправления регистра/пробелов в ключах или объединения конфликтующих версий. Если один city key должен разрешиться в нескольких обязательных локальных таблицах, v1 считает его поддержанным только при успешном разрешении всех нужных полей.

## Контракт `ClimateResolver`

На входе: `city`, `responsibility_factor`, контекст потребителя (`frame` или `window`), при необходимости `terrain_type` и нормативная ветка.

На выходе для каркаса: доказанные климатические коды/нагрузки плюс provenance.

На выходе для окна:

- сохранённый fixture может вернуть cached trace;
- произвольный город возвращает `WINDOW_CITY_DATASET_VERSION_CONFLICT`;
- никакие данные из 82,4%-кандидата не маркируются как legacy parity.


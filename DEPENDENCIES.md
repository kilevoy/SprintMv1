# Sprint M — DEPENDENCIES

## Внешние зависимости основной книги

| Link ID | Внешняя книга | Сохраненные листы | Где используется |
|---|---|---|---|
| `[1]` | `Подбор прогонов v4.3 - фикс шага.xlsx` | `Подбор прогонов`, `Расчеты`, `Расчеты МП390`, `Города п.К` | 24 внешних ссылочных токена на `Подбор прогонов`; по 57 408 токенов на каждом из `Расчеты`, `Расчеты МП390`, `Расчеты 2`, `Расчеты МП390 2`; прямые формулы используют `[1]Расчеты` и `[1]Подбор прогонов` |
| `[2]` | `Подбор прогонов v4.3.xlsx` | `Подбор прогонов`, `Расчеты`, `Расчеты МП390`, `Города п.К` | Прямых `[2]` токенов в формулах не найдено; используется через локальное имя `нормы` на листе `Подбор прогонов 2`, которое ссылается на `[2]Подбор прогонов!R35:R36` |
| `[3]` | `Таблица нагрузок по городам.xlsx` | `СП 20`, `СП РК EN` | `Лист1!J18` и `Лист7!J18`: `INDEX/MATCH` по внешнему листу `[3]СП РК EN` |
| `[4]` | `Подбор оконых ригелей.xlsx` | `Лист1`, `Расчет`, `Лист2`, `Ветер СП`, `Ветер по СП РК EN`, `типы окон` | `Расчет!D9`, `Расчет!F9`: выбор ветрового источника между `[4]Ветер по СП РК EN` и `[4]Ветер СП` |
| `[5]` | `Предрасчет колонн (СТО АСЧМ, Здания с кранами до 10т, 1- и 2-хпролетные) v4.0.xlsx` | `Подбор`, `расчет`, `Города пр.К`, `Ветер`, `Ветер EN`, `снег`, `Лист4` | `Ветер по СП РК EN!B7:B8` ссылается на `[5]Подбор!B8` и `[5]Подбор!B18` |

## Внешние ссылки через именованные диапазоны

| Имя | Область | RefersTo |
|---|---|---|
| `нормы` | локально на `Подбор прогонов` | `'[1]Подбор прогонов'!$R$35:$R$36` |
| `нормы` | локально на `Подбор прогонов 2` | `'[2]Подбор прогонов'!$R$35:$R$36` |
| `утеплитель` | глобально | `'[1]Подбор прогонов'!$Q$22:$Q$24` |

Внешние link parts находятся в `xl/externalLinks/externalLink1.xml` … `externalLink5.xml`. Внутри XLSX сохранены кэшированные значения внешних книг, но кэш не заменяет исходные книги и не позволяет подтвердить их рекурсивные зависимости.

## Основной внутренний поток

`вывод!D2:D7, D9, D20:D29`
→ `подбор` и `снегветер`
→ таблица соответствующего пролета `9м/12м/15м/18м/21м/24м`
→ `подбор!U14:AI15`
→ `вывод!D33:D57`

Отдельная ветка прогонов:

`вывод!D4:D7, D20:D27`
→ `Подбор прогонов 2`
→ `Расчеты 2` / `Расчеты МП390 2`
→ `Подбор прогонов 2!P28:V28`
→ `вывод!D35` и `D28`.

Оконная ветка:

`Лист1/Лист7`
→ `Расчет` + `Ветер СП` / `Ветер по СП РК EN`
→ `вывод!D68`.

## Формульные механизмы, обнаруженные в книге

Обнаружены `IF`, `AND`, `OR`, `INDEX`, `MATCH`, `VLOOKUP`, `SUM`, `MIN`, `MAX`, `SMALL`, `ROUND`, `ROUNDUP`, `CEILING`, `CEILING.MATH`, `FLOOR`, `PI`, `COS`, `LN`, `ABS`, `AVERAGE`, `SQRT`, `IFERROR`.

Найдено:

- точное сопоставление `MATCH(...,0)`;
- приблизительное/обратное сопоставление `MATCH(...,-1)`;
- `MATCH` с опущенным третьим аргументом, то есть Excel default approximate match;
- `INDEX` и `VLOOKUP`.

`INDIRECT`, `OFFSET`, `LOOKUP`, `HLOOKUP` и `IFS` в формульном сканировании не обнаружены.

## Нерешенные внешние вопросы

Для каждой из пяти отсутствующих книг пока `UNKNOWN / MISSING SOURCE`:

1. собственные внешние ссылки;
2. скрытые/very hidden листы;
3. именованные диапазоны и проверки данных;
4. полный сортамент и кэшированные инженерные таблицы;
5. наличие ошибок и циклических зависимостей внутри внешней книги.

## Addendum stage A: фактически предоставленные книги

В проект добавлены и отдельно проинвентаризированы `Таблица нагрузок по городам.xlsx` и `Подбор оконых ригелей v2.0.xlsx`. Поэтому формулировка выше про пять отсутствующих книг относится к первоначальному снимку основной книги и не означает, что эти две переданные книги не были исследованы.

- таблица нагрузок: структура и внутренние зависимости закрыты, но точная совместимость с кэшированным источником external link не подтверждена;
- оконные ригели v2.0: структура ветки закрыта, но версия отличается от имени файла, на которое ссылается основная книга;
- подробная актуальная классификация: `EXTERNAL_DEPENDENCIES.md`;
- интегрированный граф: `DEPENDENCY_GRAPH.md`;
- полный перечень проблем: `EXCEL_ISSUES.md`.

# Аудит входов кровли и стен

## Кровля

Статус: **ROOF_REQUIRED_INPUT**.

### `вывод!D20` — покрытие

Цепочка влияния:

`D20 → Подбор прогонов 2!B6 → B7 (собственный вес покрытия, кг/м²) → B12/B13 (нагрузка) → Расчеты 2 / Расчеты МП390 2 → P28:V28 → вывод!D35/E35 и масса прогонов → вывод!D69`.

Параллельная цепочка каркаса:

`D20 → подбор!V5 → подбор!W5 (lookup коэффициента/нагрузки в снегветер!AM:AN) → подбор и таблицы пролётов → балка, колонна, шаг рам и удельная металлоёмкость`.

Для текущего `С-П 200`: `Подбор прогонов 2!B7=38,64 кг/м²`, `B12=243,1288658728 кг/м²`, `B13=2,4312886587 кН/м²`.

### `вывод!D21` — марка настила

`D21 → вывод!Q5 → вывод!X11:X63 (lookup допустимого шага по таблице AP:AZ) → вывод!D23 (максимальный шаг) → Подбор прогонов 2!B14 → подбор профиля/шага/массы`.

Поэтому `D20` и `D21` — разные обязательные входы: материал покрытия задаёт собственный вес и нагрузку, марка настила ограничивает шаг.

## Стены

Статус: **WALL_NOT_USED** для материала стены.

Поиск по формулам, подписям, lookup и скрытым управляющим полям не выявил входа типа стенового ограждения, массы/толщины стеновой панели, стенового профлиста, утеплителя стены или фасадного материала. Найденные упоминания стены относятся к направлению ветра (`Ветер по СП РК EN!B26`) и геометрии, а не к материалу.

`вывод!D27` — только булев флаг «прогон под ограждение»: `D27 → Подбор прогонов 2!B17 → дополнительное число/масса кровельных прогонов`. Он не выбирает стеновой материал и не должен быть переименован в него.

Поле утеплителя прогонов относится к специальным вариантам кровли «наше 150/200/250 мм». Оно не является характеристикой стены.

Итог: в воспроизводимой формульной модели Core 1 отдельный wall-material input отсутствует. Если будущий продукт должен учитывать стены в BOM, это новая функция Core 2, а не скрытый legacy-вход Core 1.


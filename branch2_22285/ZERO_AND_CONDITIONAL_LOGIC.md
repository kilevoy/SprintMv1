# ZERO_AND_CONDITIONAL_LOGIC

## Методика

Проверены все формулы, содержащие текстовый шаблон `*0`, и все формулы с веткой `IF(...,0)`. Первичный текстовый поиск дал 42 ячейки, но часть из них — это десятичные коэффициенты `0.05`, `0.2`, `0.3`, а не нулевой множитель. После различения найдено 22 ячейки с точным множителем `*0` и 25 дополнительных conditional-only ячеек. Итого проверено 47 уникальных нулевых/условных ячеек.

`*0` не трактуется автоматически как «неиспользуемая ветка»: в одной формуле может быть активная часть и обнулённый добавочный член. Статус ниже означает только то, что следует из самой формулы и текущих входов.

## Полный перечень подозрительных нулевых и `*0.x` шаблонов

В таблицах ниже сохранены также активные формулы с `*0.05`, `*0.2` и подобными коэффициентами, чтобы не потерять их при последующей проверке. Они помечены как активные коэффициенты, а не как отключённые ветки.

### `12м`

| Ячейки | Формулы / текущий смысл |
|---|---|
| `C23` | `=3*C9*0` — полностью нулевая позиция ПГС 145x70x2 |
| `C24` | `=7*2*2*C9+2*C9*0` — активная часть плюс нулевой добавочный член |
| `C25` | `=2*(C9+1)*0` — отключённая позиция ПП |
| `C27` | `=I17*2*C8*0.05*1.1*2` — активный коэффициент `0.05`, не отключение |
| `C40`, `C42` | нулевые формулы обшивки/добавочного элемента |
| `C55`, `C56` | перегородки: условие по `E17`, затем явный `*0`; текущий `E17=0` |
| `C72` | `=C9*0.5*1.1` — активные коэффициенты, не отключение |
| `C74`, `C76`, `C77`, `C78` | отдельные кровельные/снеговые позиции с нулевым множителем |
| `C106`, `C107`, `C111` | защитная лента, утеплитель, Изоспан; текущая ветка обнулена |
| `C116`, `C117`, `C122` | вспомогательные крепёж/утеплитель/ГВЛ, зависят от перегородок |
| `C127` | `=I128*0.2*1.05` — активный коэффициент `0.2`, результат ненулевой |
| `C141` | `=2.5*(C8+0.5+2*C10)*K90*0` — полностью нулевая ветка |
| `L155` | `=10*5.5*0` — вспомогательная строка, не основной BOM |
| `H188` | `=E76/1.15*($E$9+FLOOR($E$9/6,1)*0.2)*1.17` — активные коэффициенты `0.2`, `1.17`; нулевой множитель отсутствует |

Текущие ненулевые активные результаты среди этих формул: `C24=1344`, `C27=51.48`, `C72=26.4`, `C127=181.44`. Остальные перечисленные ячейки в текущем сценарии кэшированы нулём.

### `15`

| Ячейки | Формулы / статус |
|---|---|
| `C27` | `=I17*2*C8*0.05*1.1` — активный коэффициент `0.05` |
| `C73` | `=C9*0.5*1.11` — активные коэффициенты |
| `C105` | `=IF(G9=4,K90*(12.5+2*C10)*0.2,0)` — условная огнезащитная ветка; при `G9=5` ноль |
| `H178` | вспомогательная формула с активными коэффициентами, текущий кэш 0 |

### `18`

| Ячейки | Формулы / статус |
|---|---|
| `C26` | `=I17*2*C8*0.05*1.1` — активный коэффициент `0.05` |
| `C73` | `=C9*0.5*1.11` — активные коэффициенты |
| `C103` | `=IF(G9=4,K90*(12.5+2*C10)*0.2,0)` — текущий `G9=5`, ветка 0 |
| `H180` | вспомогательная формула с активными коэффициентами, текущий кэш 0 |

### `21`

| Ячейки | Формулы / статус |
|---|---|
| `C27` | `=2*C8*0.05*I17` — активный коэффициент `0.05` |
| `C63` | `=C9*0.5*1.11` — активные коэффициенты |
| `C86` | активная часть формулы плюс `+J75*L139*0`; нулевой член не отключает всю позицию |
| `C94` | условная огнезащитная ветка по `G9`, текущий кэш 0 |
| `H169` | вспомогательная формула с активными коэффициентами, текущий кэш 0 |

### `1ск`

| Ячейки | Формулы / статус |
|---|---|
| `C27` | `=I17*(2*C8*0.05)*1.1` — активный коэффициент `0.05` |
| `C86` | активные члены плюс два обнулённых геометрических/добавочных члена |
| `C87` | активный уголок плюс обнулённый член |
| `C94` | условная огнезащитная ветка по `G9`, текущий кэш 0 |
| `H119` | `=14+95*0.2` — активный коэффициент `0.2`, результат 33 |
| `H168` | вспомогательная формула с активными коэффициентами, текущий кэш 0 |

## Conditional-only ветки с возвратом 0

| Листы и ячейки | Условие | Текущий режим | Класс |
|---|---|---|---|
| `12м!C41`; `15!C37,C41`; `18!C37`; `21!C37,C41`; `1ск!C37,C41` | `I3=J10` | `I3=2`, `J10=4`, поэтому 0 | переключатель выбранного типа наружной обшивки/добавочного элемента |
| `12м!G178`, `15!G168`, `18!G170`, `21!G159`, `1ск!G158` | `I4=1`, иначе проверка внешнего рамника, иначе 0 | `I4=0` | цена/масса вспомогательной ветки рамника |
| `15!C56,C57`; `18!C46,C47`; `21!C46,C47`; `1ск!C46,C47` | `E17>0` | `E17=0` | перегородки |
| `15!C105`, `18!C103`, `21!C94`, `1ск!C94` | `G9=4` | `G9=5` | огнезащитная/защитная позиция; `12м!C106` уже входит в точный `*0` список |

## A.1 значения, множители и причины

| Ячейки/ветка | calculated_value | inclusion_factor | effective_value | reason_for_zero | activation_condition |
|---|---:|---:|---:|---|---|
| `12м!C23` | `3*C9=144` | 0 | 0 | UNKNOWN ZERO LOGIC | неизвестно |
| `12м!C24` | активная часть `7*2*2*C9=1344`; добавочный член `2*C9` | 1 для основной части, 0 для добавочного | 1344 | причина нулевого добавочного члена неизвестна | неизвестно |
| `12м!C25` | `2*(C9+1)=98` | 0 | 0 | UNKNOWN ZERO LOGIC | неизвестно |
| `12м!C40/C42` | геометрическое выражение; кэш 0 | 0 | 0 | UNKNOWN ZERO LOGIC | возможно тип обшивки, но условие не доказано |
| `12м!C55/C56` | условные перегородочные выражения | `IF(E17>0,...,0)` и дополнительный `*0` | 0 | текущая площадь перегородок равна 0; дополнительный `*0` не объяснён | `E17>0` и неизвестное второе условие |
| `12м!C74,C76:C78` | кровельные/снеговые количества | 0 | 0 | UNKNOWN ZERO LOGIC | отдельные upstream cells/тип кровли |
| `12м!C106` | `IF(G9=4, ..., 0)` | условие и `*0` | 0 | `G9=5`, плюс явный нулевой множитель | `G9=4` |
| `12м!C107,C111` | выражения утеплителя/Изоспана | 0 | 0 | UNKNOWN ZERO LOGIC | неизвестно |
| `15/18/21/1ск!C37/C41` | активное выражение при совпадении кода | `IF(I3=J10,...,0)` | 0 | текущие `I3=2`, `J10=4` | `I3=J10` |
| `15!C105`, `18!C103`, `21!C94`, `1ск!C94` | защитное выражение | `IF(G9=4,...,0)` | 0 | текущий `G9=5` | `G9=4` |
| `15!C56:C57`, `18!C46:C47`, `21!C46:C47`, `1ск!C46:C47` | перегородочные quantities | `IF(E17>0,...,0)` | 0 | текущий `E17=0` | `E17>0` |
| `12м!G178`, `15!G168`, `18!G170`, `21!G159`, `1ск!G158` | цена/масса рамника | вложенный `IF(I4=1,...,IF(...,0))` | 0/unknown cache | `I4=0`; external source 10 отсутствует | `I4=1` или внешний тип рамника = 2 |
| `21!C86` | активная часть `1.328418` плюс нулевой член | 1 для активной части, 0 для `J75*L139` | 1.328418 | причина нулевого добавочного члена неизвестна | неизвестно |
| `1ск!C86/C87` | активные геометрические члены плюс нулевые члены | 1 для активных, 0 для отдельных слагаемых | `C86=1.551727`, `C87=0.384536` | UNKNOWN ZERO LOGIC для отключённых слагаемых | неизвестно |

Эти записи описывают действие текущих формул, а не предлагают удалить нули. Для закрытия причин нужны сценарные пересчёты и Core 1 outputs.

## Вывод

Ни одна из нулевых веток не может быть объявлена «мёртвой» без сценарного теста с изменением соответствующего входа и восстановления внешних источников. Доказаны только текущие состояния: перегородки выключены `E17=0`, ветка огнезащиты `G9=5` не проходит условие `=4`, а некоторые наружные ветки не проходят `I3=J10`.

# FRAME STEP — D8/D9 SEMANTICS AUDIT

Режим: read-only reverse-engineering. Production TypeScript и исходная книга
не изменялись. Контролируемые значения получены на свежих копиях книги через
Excel COM `CalculateFullRebuild()`; копии закрывались без сохранения.

Источник: `Таблица по подбору сечений теплых ангаров пролетами 9м, 12м,
15м, 18м, 21м, 24м версия 1,5.xlsx`.

SHA256 источника:
`0271b96c6fe725d3e50ad7891401958322a5ae97866bb0bf8cb190bc4bbeff3f`.

## 1. Итог

- `вывод!D8` — автоматический номинальный шаг, вычисляемый через семейство
  пролёта, высотную/климатическую ветку и локальную таблицу span-sheet.
- `вывод!D9` — ручной override. В исходной книге это не формула и для него нет
  data-validation; blank является штатным автоматическим режимом.
- `вывод!F8 = IF(D9=0,D8,D9)` — точная формула эффективного шага.
- Число пролётов/секций: `CEILING(length / effective_step)`; число рам в
  `T`-колонке: `CEILING(length / effective_step) + 1`.
- Длина 20 м не образует отдельной доказанной автоматической ветки. В текущем
  источнике не найдено условие `length=20` в цепи D8/D9; при length=20 работают
  обычные `CEILING.MATH`-формулы.
- 24 м геометрически и вычислительно достижимы в активном пересчёте. Сохранённые
  `#N/A` для 24 м относятся к stale cache при сохранённом span=12 и не являются
  общим правилом.

Статус: `D8_D9_FORMULA_SEMANTICS_PROVEN`.

`SAFE TO IMPLEMENT GENERIC NUMERIC FRAME-STEP FIX = NO`: формулы и входная
семантика доказаны, но одна универсальная таблица числовых констант для всех
книг не доказана. Изменение Core1 не выполнялось.

## 2. Полный путь D8

```text
вывод!D8
  = подбор!AA14
подбор!AA14
  = INDEX($I$2:$I$7, MATCH(AM9, $A$2:$A$7, 0))
подбор!AM9
  = IF($V$10<=9,9,AM10)
подбор!AM10
  = IF($V$10<=12,12,AM11)
подбор!AM11
  = IF($V$10<=15,15,AM12)
подбор!AM12
  = IF($V$10<=18,18,AM13)
подбор!AM13
  = IF($V$10<=21,21,AM14)
подбор!AM14
  = IF($V$10<=24,24,AM15)
подбор!V10 = вывод!D4       (literal span)
подбор!I2:I7 = ссылки на 9м/12м/15м/18м/21м/24м span-sheet
```

В span-sheet значение `I` берётся из локального lookup. Для обычных семейств
характерная форма — `18м!IG19 = INDEX(IG6:IG14, MATCH(HZ18,HZ6:HZ14,0))`,
где `HZ5 = подбор!AN9`, а `HZ18` выбирается по городу/климату и
ответственности:

```text
HZ18 = INDEX(IF(подбор!V9=0.8, снегветер!M3:M600, снегветер!K3:K600),
             MATCH(подбор!V6, снегветер!B3:B600, 0))
```

Высотная цепь `подбор!AN9`:

```text
AN9  = IF(V10>21,AN10,IF(V8<=3.8,3.6,AN10))
AN10 = IF(V10>21,AN11,IF(V8<=5,4.8,AN11))
AN11 = IF($V$8<=6.2,6,AN12)
```

Таким образом, длина здания `подбор!V11 = вывод!D5` не является прямым
precedent D8. Она влияет на число рам и на последующие массы, но не меняет D8
в автоматических сценариях ниже.

## 3. Контролируемая матрица автоматического D8

Входы эксперимента: город `Роза`, высота 3 м, ответственность 0,8, D9 blank,
стандартные roof/deck, нулевые окна. В каждой строке выполнялся полный
пересчёт свежей копии.

| literal span / family | D8 = F8 (m) | длины, проверенные в auto | T при длине 18 / 20 / 24 / 25.7 / 26 / 30 |
|---:|---:|---|---|
| 9 / 9 | 6.0 | 18 | 4 / — / — / — / — / — |
| 12 / 12 | 6.0 | 18, 20, 24, 25.7, 26, 30 | 4 / 5 / 5 / 6 / 6 / 6 |
| 15 / 15 | 6.0 | 18, 20, 24, 25.7, 26, 30 | 4 / 5 / 5 / 6 / 6 / 6 |
| 18 / 18 | 5.0 | 18, 20, 24, 25.7, 26, 30 | 5 / 5 / 6 / 7 / 7 / 7 |
| 21 / 21 | 4.0 | 18, 20, 24, 25.7, 26, 30 | 6 / 6 / 7 / 8 / 8 / 9 |
| 24 / 24 | 4.3 | 18 | 6 / — / — / — / — / — |

Для 24 м контрольный сценарий дал: `24м!KL5=6`, `KL18=1`,
`подбор!AA14=4.3`, `AA15=4.3`, `E8=E9=35.73278736728395`,
`D69=35.73278736728395`. Это подтверждает активный numeric путь, а не
unconditional `#N/A`.

## 4. D9 и эффективный шаг

`вывод!D9` в источнике — blank cell (raw formula отсутствует). Data validation
для D9 отсутствует. Excel трактует blank как 0 в условии:

```excel
вывод!F8 = IF(D9=0,D8,D9)
```

Связь с подбором:

```excel
подбор!W2 = вывод!D9
Подбор прогонов!B1  = вывод!D8
Подбор прогонов 2!B1 = вывод!F8
```

При ненулевом D9 второй блок использует ручной знаменатель напрямую:

```excel
подбор!S9:S14 = IF($W$2=0,CEILING.MATH($V$11/Irow)-1,
                         CEILING.MATH($V$11/$W$2)-1)
подбор!T9:T14 = IF($W$2=0,CEILING.MATH($V$11/Irow)+1,
                         CEILING.MATH($V$11/$W$2)+1)
```

Контрольные manual length=20:

| span | D8 | D9 | F8 | E8 | E9 | D69 | T (effective branch) |
|---:|---:|---:|---:|---:|---:|---:|---:|
| 12 | 6.0 | 4.0 | 4.0 | 31.206439583333335 | 29.31727291666667 | 29.31727291666667 | 6 |
| 15 | 6.0 | 4.0 | 4.0 | 30.852951666666666 | 28.771618333333336 | 28.771618333333336 | 6 |
| 18 | 5.0 | 4.5 | 4.5 | 27.013516666666668 | 29.750072222222226 | 29.750072222222226 | 6 |
| 21 | 4.0 | 4.0 | 4.0 | 26.628571428571426 | 26.628571428571426 | 26.628571428571426 | 6 |

The `T` result is total frame count. `S` is the corresponding internal count
(`T-2`), not the displayed total frame count.

## 5. Frame-count formulas and boundaries

Automatic block (rows 2:7):

```excel
подбор!S2:S7 = CEILING.MATH($V$11/Irow)-1
подбор!T2:T7 = CEILING.MATH($V$11/Irow)+1
```

Effective/manual block (rows 9:14):

```excel
подбор!S9:S14 = IF($W$2=0,CEILING.MATH($V$11/Irow)-1,
                         CEILING.MATH($V$11/$W$2)-1)
подбор!T9:T14 = IF($W$2=0,CEILING.MATH($V$11/Irow)+1,
                         CEILING.MATH($V$11/$W$2)+1)
```

Следовательно, при точной кратности длины число рам равно `length/step + 1`,
а при некратной длине Excel округляет число bays вверх и добавляет две
концевые рамные позиции относительно `S`.

## 6. Связь D8/D9 с E8/E9 и D69

```excel
вывод!E8 = подбор!Z14 + 'Подбор прогонов'!$T$28
вывод!E9 = подбор!Z15 + 'Подбор прогонов 2'!$T$28
вывод!D69 = IF(D9=0,E8,E9) + D68
```

В auto D9 blank/0 выбирает E8. При ненулевом D9 выбирается E9; E8 при этом
всё ещё рассчитывается и остаётся доступным для сравнения.

Отдельно `вывод!D22 = IF(E8>E9,подбор!AA15,подбор!AA14)` — это display selector
по меньшей структурной массе, а не замена D8 и не override D9.

## 7. 20 м и специальные случаи

Проверенные auto length=20 сценарии для семейств 12/15/18/21 оставили D9 blank,
F8 равным D8 и изменили только S/T и downstream mass. В текущей исходной книге
`подбор!F9` является ссылкой на `9м!RT19`, а не executable `length=20` switch.
Отдельное текстовое условие «длина 20 м» в цепи D8/D9 не найдено. Поэтому
`длина 20` классифицируется как manual-guidance/legacy context, но не как
доказанная автоматическая ветка.

## 8. Сравнение с Core1 и реальными проектами

Текущий `src/core1/frame/FrameSelector.ts` содержит константную карту:

```text
9→6, 12→6, 15→4, 18→4.5, 21→4, 24→null
```

Она не является доказанной универсальной заменой Excel D8: в контролируемой
книге master получено `15→6`, `18→5`, `24→4.3`; это первая divergence
относительно текущей Core1 карты для 15/18/24. Реальные книги уже показывали
проектные значения `22318: 4`, `22316: 4.5`, `22329: 6`, `22326: 6` при
одинаковом blank D9. Следовательно, numeric D8 — workbook/table-dependent
result, а не только literal span.

`frame_step_override_m` в Core1 правильно моделирует явный D9 override, но
аудит не меняет проверку допустимых ручных шагов и не доказывает generic
таблицу для всех workbook versions.

## 9. Вывод и безопасная граница реализации

1. `D8` semantics: **PROVEN as formula chain**, numeric generic map — **not
   proven**.
2. `D9` semantics: **PROVEN** as blank-or-numeric manual override.
3. Effective step `F8`: **PROVEN** exact switch.
4. Frame counts `S/T`: **PROVEN** exact `CEILING.MATH` formulas.
5. Length 20: no separate executable automatic branch proven.
6. 24 m: active local calculation can be numeric; stale saved `#N/A` must not be
   converted into an unconditional span error.
7. Core1 production code: **not changed**.
8. XLSX: **not changed**.

Final classification: `FRAME_STEP_D8_D9_SEMANTICS_PROVEN`.
Implementation of a generic numeric selector remains a separate, evidence-led
task; this audit intentionally does not implement it.

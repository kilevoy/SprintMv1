# Аудит ветровой ветки окон

## External ID 3: город и нормативная ветка

| Cell | Формула / значение | Роль |
|---|---|---|
| `Лист1!J18` | `INDEX('[3]СП РК EN'!C3:C1000; MATCH(B2;'[3]СП РК EN'!B3:B1000;0);1)`; cache `#N/A` | точная проверка наличия города и получение значения из колонки C |
| `Лист1!K18` | `IFERROR(J18;"нет")`; cache `нет` | преобразование lookup-ошибки в флаг |
| `Лист1!J19` | `IF(K18="нет";"нет";"да")`; cache `нет` | Kazakhstan-флаг |

`J19` имеет 820 прямых потребителей: `Расчет!AK4:AK413` и `AN4:AN413`. В них используется делитель `IF(Лист1!J19="да";1/1,05;1)`. Таким образом, коэффициент `1/1,05` меняет проверки нижнего и верхнего оконных ригелей, когда город найден в таблице `СП РК EN`.

Нормативные варианты также представлены скрытыми настройками `Лист1!D15:D16` (`по СП 20...` / `по СП РК EN`), но фактический Казахстан-флаг для коэффициента берётся из `J19`.

Алгоритм полностью воспроизводим без Excel, если в Core 1 перенести точную таблицу `[3]СП РК EN!B:C` и сохранить exact-match/first-match поведение. Точная legacy-версия не подтверждена: локальная `Таблица нагрузок по городам.xlsx` совпала с cache проверяемой версии в 1 142 из 1 386 ячеек, 244 отличаются. Поэтому произвольный город пока не гарантирует parity 1:1.

## External ID 4: `Расчет!D9/F9`

Исходные формулы основной книги:

- `D9 = IF(Лист1!J20="да"; -'[4]Ветер по СП РК EN'!D28; '[4]Ветер СП'!G25) * Лист1!B3`
- `F9 = IF(Лист1!J20="да"; -'[4]Ветер по СП РК EN'!D29/1,5; '[4]Ветер СП'!G26/1,4) * Лист1!B3`

External cache `[4]` содержит:

| Cell | Cached external value | Текущее локальное значение | Вывод |
|---|---:|---|---|
| `Ветер СП!G25` | `0,43969114285714284` | `#N/A` | локальная формула есть, но текущий local input lookup не разрешился |
| `Ветер СП!G26` | `0,3517529142857142` | `#N/A` | то же |
| `Ветер по СП РК EN!D28` | `#N/A` | `#N/A` | одинаковое состояние ошибки, не доказано равенство для валидного города |
| `Ветер по СП РК EN!D29` | `#N/A` | `#N/A` | то же |

Сохранённые результаты `Расчет!D9=0,43969114285714284`, `F9=0,251252081632653` показывают, что при пустом `J20` Excel выбрал ветку `Ветер СП`; второе значение равно `G26/1,4`.

Локальный лист `Ветер СП` структурно содержит все 231 формулу, совпадающие с формулами книги `Подбор оконых ригелей v2.0.xlsx`, но это не доказывает равенство старой `[4]`: у v2.0 `G25=0,730096984064`, `G26=0,5840775872512`, а её `Расчет!D9/F9` дополнительно складывают компоненты (`G25+G35`, `D28+C45`). Это новая методика или версия, а не drop-in replacement. Разницу external cache и local values нельзя однозначно разделить на разные inputs, устаревший cache и изменение методики без оригинальной книги `[4]`.

## External ID 5 и `J20`

- `Ветер по СП РК EN!B7 = [5]Подбор!B8`, cache `6`.
- `Ветер по СП РК EN!B8 = [5]Подбор!B18`, cache `66`.
- `B7` участвует в `G14`, `E26`, `G30`, `Z32`, `Z39`; `B8` — в `E13`, `G17`, `G27`, `AC32`, `AC39`. Эти цепочки формируют локальные EN-значения, включая `D28:D29`.

Однако `Лист1!J20` пуст, не имеет формулы, Data Validation или иного обнаруженного производителя. Единственные потребители — условия `Расчет!D9/F9`. Известный вычисляемый Kazakhstan-флаг расположен в `J19`, не `J20`.

Классификация:

- `J19`: **ACTIVE**;
- `J20`: **FORMULA BUG**; возможное историческое назначение — **MANUAL LEGACY SWITCH**, но доказательств пользовательского управления нет;
- ветка `[4]Ветер по СП РК EN` и downstream ID 5 при нормальном состоянии книги: **UNREACHABLE**;
- `[5]B7:B8` при принудительном `J20="да"`: **ACTIVE_REQUIRED**, но их семантика и исходная книга остаются неизвестными.

## Решение для Core 1

Формульный алгоритм можно реализовать без Excel runtime. Строгий legacy parity для всех городов и обеих нормативных веток пока заблокирован отсутствием точных таблиц `[3]` и старой версии `[4]`, а также неоднозначностью `J19/J20`. Ничего автоматически не исправлять: compatibility mode должен повторять обращение к `J20`, включая недостижимость EN-ветки; corrected mode потребует отдельного инженерного решения.

